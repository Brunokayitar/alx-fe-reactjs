const ProfileSettings = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold">Profile Settings</h2>
      <p>Change your password, update preferences, etc.</p>
    </div>
  );
};

export default ProfileSettings;
